import React from "react";
import { AccountRegisterForm, Setup } from "@/client/auth";

const Page = () => {
  return (
    <div>
      <Setup />
      <AccountRegisterForm />
    </div>
  );
};

export default Page;
